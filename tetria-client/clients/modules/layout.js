/**
 * Provides structure for creating a layout of html in different views.
 * 
 * Note: Resizing changes which don't change HTML structure are NOT controlled by this.
 */

tet.modules.layout = {
	state: {}
}
console.log("loaded layout!");